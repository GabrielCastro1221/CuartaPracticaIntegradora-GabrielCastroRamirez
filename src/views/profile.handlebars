<div class="container-fluid mt-5 py-5">
  <h1 class="text-center text-success fs-4 text-uppercase">Mi perfil</h1>
  <div class="container px-5 rounded">
    <hr class="mt-3 border border-secondary">
    <form>
      <div class="d-flex gap-5">
        <div class="d-flex align-items-center w-25">
          <p class="fs-6 text-success text-uppercase fw-bold">Nombre:</p>
        </div>
        <div class="w-75">
          <div>
            <h2 class="fs-6 text-secondary">{{user.name}}</h2>
          </div>
          </label>
          <input type="file" id="avatar" class="d-none">
        </div>
      </div>
      <hr class="mt-3 border border-secondary">
      <div class="d-flex gap-5">
        <div class="d-flex align-items-center w-25">
          <p class="fs-6 text-success text-uppercase fw-bold">Apellido:</p>
        </div>
        <div class="w-75">
          <div>
            <h2 class="fs-6 text-secondary">{{user.surName}}</h2>
          </div>
          </label>
          <input type="file" id="avatar" class="d-none">
        </div>
      </div>
      <hr class="mt-3 border border-secondary">
      <div class="d-flex gap-5">
        <div class="d-flex align-items-center w-25">
          <p class="fs-6 text-success text-uppercase fw-bold">Email:</p>
        </div>
        <div class="w-75">
          <div>
            <h2 class="fs-6 text-secondary">{{user.email}}</h2>
          </div>
          </label>
          <input type="file" id="avatar" class="d-none">
        </div>
      </div>
      <hr class="mt-3 border border-secondary">
      <div class="d-flex gap-5">
        <div class="d-flex align-items-center w-25">
          <p class="fs-6 text-success text-uppercase fw-bold">Rol:</p>
        </div>
        <div class="w-75">
          <div>
            <h2 class="fs-6 text-secondary">{{user.rol}}</h2>
          </div>
          </label>
          <input type="file" id="avatar" class="d-none">
        </div>
      </div>
      <hr class="mt-3 border border-secondary">
  </div>
  </form>
</div>

